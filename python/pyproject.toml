[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-memory-runtime"
version = "0.1.0"
description = "Filesystem-style Python client for SharedMemoryRuntime"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["shared_memory_runtime"]
